# Інструкція з встановлення та використання HR Hospital

## 📦 Встановлення

### Вимоги
- Odoo 17.0
- Python 3.10+
- PostgreSQL 12+

### Крок 1: Копіювання модуля

```bash
# Скопіюйте модуль до директорії з аддонами
cp -r hr_hospital /path/to/odoo/addons/

# АБО створіть symbolic link
ln -s /path/to/hr_hospital /path/to/odoo/addons/hr_hospital
```

### Крок 2: Оновлення списку модулів

У Odoo перейдіть до:
```
Apps → ⋮ (меню) → Update Apps List
```

### Крок 3: Встановлення

1. Знайдіть "Hospital Management" у списку Apps
2. Натисніть **Install**
3. Дочекайтесь завершення встановлення

### Крок 4: Демо дані (опціонально)

Для встановлення з демо даними:
```bash
odoo-bin -c odoo.conf -d your_database -i hr_hospital --load-language=uk_UA --without-demo=False
```

---

## 🔄 Оновлення існуючої версії

### Backup перед оновленням

**ОБОВ'ЯЗКОВО** створіть резервну копію:

```bash
# Backup бази даних
pg_dump your_database > backup_$(date +%Y%m%d_%H%M%S).sql

# Backup файлів
tar -czf hr_hospital_backup.tar.gz /path/to/odoo/addons/hr_hospital
```

### Оновлення модуля

```bash
# Через odoo-helper
odoo-helper -c odoo.conf -u hr_hospital

# АБО через odoo-bin
odoo-bin -c odoo.conf -d your_database -u hr_hospital

# З перезапуском
odoo-bin -c odoo.conf -d your_database -u hr_hospital --stop-after-init
```

### Після оновлення

1. Перевірте логи на помилки
2. Оновіть "Update Apps List"
3. Перезавантажте сервер Odoo
4. Перевірте функціонал

---

## 📘 Перший запуск - Quick Start

### 1. Створення спеціальностей

```
Hospital → Configuration → Specializations → Create
```

Приклади:
- Cardiology
- Neurology
- Pediatrics
- General Practice

### 2. Додавання лікарів

```
Hospital → Doctors → Create
```

Заповніть:
- ПІБ (прізвище, ім'я, по батькові)
- Email та телефон
- Спеціальність
- Ліцензійний номер
- Рейтинг (0-5)

### 3. Створення пацієнтів

```
Hospital → Patients → Create
```

Заповніть:
- ПІБ
- Контакти
- Дата народження
- Алергії (якщо є)
- Персональний лікар

### 4. Генерація розкладу

```
Hospital → Configuration → Generate Schedule
```

Виберіть:
- Лікаря
- Початок тижня
- Кількість тижнів
- Дні роботи
- Робочі години

### 5. Створення візитів

```
Hospital → Visits → Create
```

Або використайте фільтри для пошуку доступних лікарів.

---

## 🧙 Wizards - Детальні інструкції

### Mass Reassign Doctor Wizard

**Призначення:** Масове перепризначення пацієнтів від одного лікаря до іншого.

**Доступ:**
```
Hospital → Patients → Action → Mass Reassign Doctor
```

**Кроки:**
1. Виберіть старого лікаря (From Doctor)
2. Виберіть нового лікаря (To Doctor)
3. Опціонально: фільтр за країною
4. Вкажіть дату зміни
5. Вкажіть причину
6. Натисніть "Reassign"

**Результат:**
- Всі пацієнти перепризначені
- Створені записи в історії
- Notification з кількістю пацієнтів

---

### Disease Report Wizard

**Призначення:** Генерація звітів по захворюваннях за період.

**Доступ:**
```
Hospital → Reports → Disease Report
```

**Налаштування:**

1. **Фільтри:**
   - Лікарі (Multiple)
   - Хвороби (Multiple)
   - Країни (Multiple)
   - Період (Date From - Date To)

2. **Групування:**
   - По лікарю
   - По хворобі
   - По країні

3. **Тип звіту:**
   - Detailed (детальний список)
   - Summary (підсумки з підрахунками)

**Приклад використання:**

```
Період: 01.01.2025 - 31.01.2025
Фільтр: Хвороби = Flu, COVID-19
Групування: doctor
Тип: summary
```

**Результат:**
- Notification з підсумками
- Кількість діагнозів по лікарях

---

### Reschedule Visit Wizard

**Призначення:** Перенесення візиту на іншу дату або до іншого лікаря.

**Доступ:**
```
Hospital → Visits → [вибрати візит] → Action → Reschedule Visit
```

**Кроки:**
1. Поточний візит (відображається readonly)
2. Виберіть нового лікаря (або залиште поточного)
3. Вкажіть нову дату
4. Вкажіть новий час (float формат, наприклад 14.5 = 14:30)
5. Вкажіть причину перенесення
6. Натисніть "Reschedule"

**Що відбувається:**
- Старий візит видаляється
- Створюється новий візит з новими параметрами
- Зберігаються рекомендації
- Зберігається причина

---

### Doctor Schedule Wizard

**Призначення:** Автоматична генерація розкладу лікаря на N тижнів.

**Доступ:**
```
Hospital → Configuration → Generate Schedule
```

**Налаштування:**

1. **Основні:**
   - Лікар (тільки зі спеціальністю)
   - Початок тижня (Date)
   - Кількість тижнів (1-52)

2. **Тип розкладу:**
   - Standard (всі тижні)
   - Even Week (парні тижні)
   - Odd Week (непарні тижні)

3. **Дні роботи:**
   - ☑ Понеділок
   - ☑ Вівторок
   - ☑ Середа
   - ☑ Четвер
   - ☑ П'ятниця
   - ☐ Субота
   - ☐ Неділя

4. **Робочі години:**
   - Час початку (float, наприклад 9.0 = 09:00)
   - Час закінчення (float, наприклад 17.0 = 17:00)
   - Перерва від (опціонально, наприклад 13.0)
   - Перерва до (опціонально, наприклад 14.0)

**Приклад:**
```
Лікар: Dr. Smith
Початок: 11.11.2025 (понеділок)
Тижнів: 4
Тип: Standard
Дні: Пн-Пт
Години: 09:00 - 17:00
Перерва: 13:00 - 14:00
```

**Результат:**
- 20 записів розкладу (5 днів × 4 тижні)
- Notification про створені/пропущені записи

---

### Patient Card Export Wizard

**Призначення:** Експорт повної медичної картки пацієнта.

**Доступ:**
```
Hospital → Patients → [вибрати пацієнта] → Action → Export Medical Card
```

**Налаштування:**

1. **Період:**
   - Date From (опціонально)
   - Date To (опціонально)
   - Якщо не вказані - експортуються всі візити

2. **Опції:**
   - ☑ Include Diagnoses
   - ☑ Include Recommendations
   - Мова звіту (auto-detect або ручний вибір)

3. **Формат:**
   - JSON (структурований, для API)
   - CSV (таблиця, для Excel)

**JSON формат:**
```json
{
  "patient": {
    "name": "Іванов Іван Іванович",
    "age": 35,
    "doctor": "Dr. Smith"
  },
  "visits": [
    {
      "date": "2025-11-10 10:00:00",
      "doctor": "Dr. Smith",
      "status": "completed",
      "recommendations": "...",
      "diagnoses": [...]
    }
  ]
}
```

**CSV формат:**
```
Таблиця з інформацією про пацієнта
+ Таблиця візитів з діагнозами
```

**Використання:**
1. Натисніть "Export"
2. Файл згенерується
3. Натисніть для завантаження

---

## 🔍 Domains та Filtering - Практичне використання

### Статичні домени

#### 1. Візити - тільки ліцензовані лікарі

При створенні візиту автоматично показуються **тільки лікарі з license_number**.

**Немає дій користувача** - працює автоматично.

#### 2. Контактні особи - тільки пацієнти з алергіями

При створенні контактної особи показуються **тільки пацієнти з заповненим полем allergies**.

#### 3. Діагнози - тільки завершені візити

При створенні діагнозу показуються **тільки візити зі статусом "completed"**.

#### 4. Ментор - тільки не інтерни

При виборі ментора показуються **тільки лікарі з is_intern = False**.

#### 5. Діагнози - небезпечні заразні хвороби

При виборі хвороби для діагнозу показуються **тільки заразні хвороби з рівнем high або critical**.

#### 6. Розклад - лікарі зі спеціальністю

При створенні розкладу показуються **тільки лікарі з заповненою specialization_id**.

---

### Динамічні домени

#### Вибір лікаря за спеціальністю та розкладом

**Де:** Форма створення візиту

**Як працює:**

1. **Фільтр по спеціальності:**
   ```
   Hospital → Visits → Create
   → Specialization Filter: виберіть спеціальність
   → Поле Doctor автоматично фільтрується
   ```

2. **Фільтр по даті:**
   ```
   → Available Date Filter: виберіть дату
   → Показуються тільки лікарі з розкладом на цю дату
   ```

3. **Комбінація:**
   ```
   → Виберіть спеціальність + дату
   → Показуються тільки лікарі зі спеціальністю, які працюють цього дня
   ```

**Приклад:**
```
Specialization Filter: Cardiology
Available Date Filter: 15.11.2025
→ Показуються тільки кардіологи, які працюють 15.11.2025
```

---

### Search Filters з груповуванням

#### Пошук лікарів

```
Hospital → Doctors → Filters
```

**Доступні фільтри:**
- Ukraine / USA / Germany (по країні навчання)
- With Specialization (тільки зі спеціальністю)
- With License (тільки з ліцензією)
- Not Interns (без інтернів)

**Групування:**
- Group by Specialization
- Group by Country
- Group by Is Intern

**Приклад:**
```
Filters: Ukraine + With Specialization
Group by: Specialization
→ Українські лікарі, згруповані по спеціальностях
```

---

#### Пошук пацієнтів

```
Hospital → Patients → Filters
```

**Доступні фільтри:**
- Ukrainian Speakers (мова uk_UA)
- English Speakers (мова en_US)
- Ukraine Citizens (країна UA)
- USA Citizens (країна US)
- With Allergies (є алергії)
- With Doctor (є персональний лікар)

**Групування:**
- Group by Doctor
- Group by Country
- Group by Language
- Group by Gender

**Приклад:**
```
Filters: Ukrainian Speakers + With Allergies
Group by: Doctor
→ Україномовні пацієнти з алергіями, згруповані по лікарях
```

---

#### Пошук візитів

```
Hospital → Visits → Filters
```

**Доступні фільтри:**
- **За датою:**
  - Today (візити сьогодні)
  - This Week (поточний тиждень)
  - Last 30 Days (останні 30 днів)
- **За статусом:**
  - Scheduled
  - Completed
  - Cancelled
- **Інші:**
  - Weekdays Only (виключити вихідні)

**Групування:**
- Group by Doctor
- Group by Patient
- Group by Status
- Group by Visit Type
- Group by Date

**Приклад:**
```
Filters: This Week + Scheduled + Weekdays Only
Group by: Doctor
→ Заплановані візити на робочі дні цього тижня по лікарях
```

---

## 🔧 Налаштування та конфігурація

### Меню структура

```
Hospital (головне меню)
├── Patients
├── Doctors
├── Diseases
├── Visits
├── Diagnoses
├── Reports
│   └── Disease Report
└── Configuration
    ├── Specializations
    ├── Doctor Schedule
    └── Generate Schedule
```

### Права доступу

Всі моделі доступні для `base.group_user`.

Для обмеження доступу створіть окремі групи:
```python
# У security/hr_hospital_security.xml
<record id="group_hospital_doctor" model="res.groups">
    <field name="name">Hospital: Doctor</field>
</record>
```

### Налаштування email notifications

```python
# У моделях додайте:
_inherit = ['mail.thread']

# У полях:
field_name = fields.Char(tracking=True)
```

---

## 🐛 Troubleshooting

### Модуль не з'являється в Apps

**Рішення:**
1. Перевірте шлях до модуля
2. Перевірте `__manifest__.py`
3. Перезапустіть Odoo
4. Update Apps List

### Помилка при встановленні

**Перевірте:**
```bash
# Логи Odoo
tail -f /var/log/odoo/odoo.log

# Права доступу
ls -la /path/to/odoo/addons/hr_hospital

# База даних
psql -d your_database -c "SELECT * FROM ir_module_module WHERE name='hr_hospital';"
```

### Демо дані не завантажуються

**Рішення:**
```bash
# Перевстановлення з демо
odoo-bin -c odoo.conf -d your_database -i hr_hospital --without-demo=False

# Або
odoo-bin -c odoo.conf -d your_database -u hr_hospital --load-language=uk_UA
```

### Wizard не відкривається

**Перевірте:**
1. Права доступу в `ir.model.access.csv`
2. View правильно прив'язаний до action
3. Binding model правильний
4. Логи на помилки Python

### Domain не фільтрує

**Перевірте:**
1. Синтаксис domain (квадратні дужки, кортежі)
2. Імена полів (точні назви)
3. @api.onchange працює (return {'domain': ...})
4. Контекст передається правильно

---

## 📊 Перевірка працездатності

### Тестування базового функціоналу

```python
# Python shell
env = api.Environment(cr, uid, {})

# 1. Створення лікаря
doctor = env['hr.hospital.doctor'].create({
    'last_name': 'Smith',
    'first_name': 'John',
    'email': 'john@example.com',
    'license_number': 'LIC12345',
})

# 2. Створення пацієнта
patient = env['hr.hospital.patient'].create({
    'last_name': 'Doe',
    'first_name': 'Jane',
    'doctor_id': doctor.id,
})

# 3. Створення візиту
visit = env['hr.hospital.visit'].create({
    'patient_id': patient.id,
    'doctor_id': doctor.id,
    'scheduled_date': fields.Datetime.now(),
    'status': 'scheduled',
})

# 4. Перевірка
assert doctor.full_name == 'Smith John'
assert patient.doctor_id == doctor
assert visit.status == 'scheduled'
```

### Тестування wizards

```python
# Mass Reassign
wizard = env['mass.reassign.doctor.wizard'].create({
    'old_doctor_id': old_doctor.id,
    'new_doctor_id': new_doctor.id,
    'assignment_date': fields.Date.today(),
})
wizard.action_reassign()

# Export
wizard = env['patient.card.export.wizard'].create({
    'patient_id': patient.id,
    'export_format': 'json',
})
wizard.action_export()
assert wizard.export_file
```

---

## 🎓 Додаткові ресурси

### Документація
- README.md - огляд модуля
- SUMMARY.md - підсумок виконаної роботи
- CHANGELOG.md - історія версій

### Онлайн
- 🔗 GitHub: https://github.com/zhatrus/hr_hospital
- 📖 Odoo Documentation: https://www.odoo.com/documentation/17.0/

### Підтримка
- 📧 GitHub Issues: https://github.com/zhatrus/hr_hospital/issues
- 💬 Discussions: https://github.com/zhatrus/hr_hospital/discussions

---

## ✅ Checklist після встановлення

- [ ] Модуль встановлено без помилок
- [ ] Демо дані завантажені (якщо потрібно)
- [ ] Створено мінімум 1 спеціальність
- [ ] Створено мінімум 1 лікаря
- [ ] Створено мінімум 1 пацієнта
- [ ] Згенеровано розклад для лікаря
- [ ] Створено тестовий візит
- [ ] Протестовано wizard (будь-який)
- [ ] Перевірено filters та domains
- [ ] Експортовано медичну картку

---

**Модуль готовий до використання! 🚀**

Версія: 17.0.2.3.1  
Автор: Khatrus Zakhar  
Дата: 2025-11-10
